packageName: "sense-extension-utils"
wbfolder:
  enabled: false  # Doesn't make much sense to have this enabled in Qlik Dev Hub
npm:
  publish: false  # Should be false until published
buildReleaseDir: './dist'
